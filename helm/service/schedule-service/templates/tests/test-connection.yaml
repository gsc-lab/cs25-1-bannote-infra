apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "schedule-service.fullname" . }}-test-connection"
  labels:
    {{- include "schedule-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "schedule-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
