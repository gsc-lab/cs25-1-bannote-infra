apiVersion: v1
kind: Service
metadata:
  name: {{ include "studyroom-service.fullname" . }}
  labels:
    {{- include "studyroom-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "studyroom-service.selectorLabels" . | nindent 4 }}
