apiVersion: v1
    kind: Secret
    metadata:
        name: {{ include "token-service.fullname" . }}-secrets
    labels:
        {{- include "token-service.labels" . | nindent 4 }}
    type: Opaque
    data:
        jwt-secret: {{ .Values.secrets.jwtSecret | b64enc | quote }}